General Notes:

    use a dictionary to store game default values e.g., (max_hp, level, room_id)
    instantiate player objects at a starting location with default values accessed from dictionary

Name-filter:

    Filter out list of un-usable names from a dictionary. e.g., npc's, imm's

Logs:

    Needs accurate logging/reporting system

Login:

    password module
    writing to mysql
    connecting to database to store/retrieve username/password
    setting up secure login
